# Use exact versions for all dependencies (no ^ or ~)
save-exact=true

# Use pnpm's strict peer dependencies
strict-peer-dependencies=true

# Hoist all dependencies to root node_modules
shamefully-hoist=false
